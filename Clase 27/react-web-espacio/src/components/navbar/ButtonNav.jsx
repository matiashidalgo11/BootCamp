import React, {Link} from "react";

export default function ButtonNav (props) {

    
    return(
        <Link to="/">Home</Link>
        
    );
};
 {/* <Link key={props.data.key}  to={props.data.route}> <span className={props.data.styleNumber}>{props.data.number}</span> {props.data.title} </Link> */}